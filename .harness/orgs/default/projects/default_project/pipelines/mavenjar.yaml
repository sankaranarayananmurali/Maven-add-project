pipeline:
  name: mavenjar
  identifier: mavenjar
  projectIdentifier: default_project
  orgIdentifier: default
  tags:
    ai_generated: "true"
  variables:
    - name: imageTag
      type: String
      value: <+input>.default("latest")
  stages:
    - stage:
        name: maven-deploy
        identifier: maven_deploy
        type: Deployment
        failureStrategies:
          - onFailure:
              errors:
                - AllErrors
              action:
                type: StageRollback
        spec:
          deploymentType: Kubernetes
          service:
            serviceRef: mavenjarservice
          environment:
            environmentRef: mavenjarenv
            deployToAll: false
            infrastructureDefinitions:
              - identifier: mavenjarinf
          execution:
            steps:
              - step:
                  identifier: rollout_deployment
                  name: Kubernetes Rolling Deployment
                  type: K8sRollingDeploy
                  timeout: 10m
                  spec:
                    skipDryRun: false
                    pruningEnabled: false
            rollbackSteps:
              - step:
                  identifier: rollback_rollout_deployment
                  name: Rollback Kubernetes Deployment
                  type: K8sRollingRollback
                  timeout: 10m
                  spec:
                    pruningEnabled: false
                    delegateSelectors: []
  description: This pipeline was generated by Harness AI on 2025-12-18 05:42:42 UTC by sankaranarayanan.m.
